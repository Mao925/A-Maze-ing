# A-Maze-ing 実装手順書

## 目的と完了条件

`python3 a_maze_ing.py config.txt` で設定を検証し、指定モードの迷路を生成・検証・16進数形式で保存・可視化できる状態を完成とします。最終成果物は lint / 型検査 / テスト / `maze_analyzer.py` を通過し、`mazegen-*` パッケージをソースから再ビルドできるものにします。

## 1. 初期セットアップ

1. Python 3.10 以上の仮想環境を作成する。
2. `requirements-dev.txt`（または `pyproject.toml`）に `flake8`、`mypy`、`pytest`、ビルド用の `build` を定義する。
3. `.gitignore` に `__pycache__/`、`.pytest_cache/`、`.mypy_cache/`、`.venv/`、`dist/` を追加する。
4. 最低限の構成を作る。

   ```text
   .
   ├── a_maze_ing.py
   ├── config.txt
   ├── maze_analyzer.py
   ├── mazegen/
   │   ├── __init__.py
   │   └── generator.py
   ├── tests/
   ├── Makefile
   ├── pyproject.toml
   ├── README.md
   └── LICENSE.md
   ```

5. Makefile に `install`、`run`、`debug`、`clean`、`lint`、任意で `lint-strict` を実装する。

**確認:** `make install`、`make lint`、`pytest` が空の土台でも実行できること。

## 2. データモデルを先に固定する

実装の中心はセルの壁情報です。方向とビット値を1か所に集約し、壁の更新は必ず双方向に反映します。

| 方向 | 座標差 `(dx, dy)` | 壁ビット | 反対方向 |
| --- | --- | --- | --- |
| `N` | `(0, -1)` | `0b0001` | `S` |
| `E` | `(1, 0)` | `0b0010` | `W` |
| `S` | `(0, 1)` | `0b0100` | `N` |
| `W` | `(-1, 0)` | `0b1000` | `E` |

推奨する内部表現は「全セルの4壁を閉鎖した `list[list[int]]`」です。壁を開ける操作は `remove_wall(cell, direction)` に集約し、隣接セルの反対壁も同時に開けます。

最低限の型・クラス例:

```python
@dataclass(frozen=True)
class Point:
    x: int
    y: int

class MazeGenerator:
    def __init__(self, width: int, height: int, seed: int | None = None) -> None: ...
    def generate(self, perfect: bool) -> None: ...
    def shortest_path(self, start: Point, goal: Point) -> list[str]: ...
```

**確認:** 1×1、隅、中央、迷路外座標の扱いをユニットテストで固めること。

## 3. 設定ファイルを実装・検証する

1. `#` 行と空行を無視し、残りを `KEY=VALUE` として読む。
2. 必須キー（`WIDTH`、`HEIGHT`、`ENTRY`、`EXIT`、`OUTPUT_FILE`、`PERFECT`）の重複・欠落・不正キーを判定する。
3. 整数、座標、真偽値を専用関数で変換する。`PERFECT` は受け入れる文字列を明示的に定義する（例: `true` / `false` のみ）。
4. 幅・高さの下限、入口／出口が範囲内かつ異なること、`42` を置けないサイズの場合のメッセージを検証する。
5. 任意キー `SEED` を追加し、同じ設定なら同じ迷路になることを保証する。

**確認:** 不在ファイル、`=` のない行、数値でないサイズ、範囲外座標、同一入出口、無効な真偽値で、例外トレースではなく利用者向けエラーが表示されること。

## 4. 完全迷路を生成する

再現性があり、全セル連結かつループなしになる **DFS 再帰バックトラッカー**を第一実装に選びます。

1. すべてのセルを閉鎖壁で初期化する。
2. `random.Random(seed)` を生成器専用の乱数源として使う。
3. 開始セルから未訪問の隣接セルをランダムに選び、両セル間の壁を開けて進む。
4. 未訪問隣接セルがなければスタックを戻る。
5. 全セルを訪問して完了する。
6. BFS で入口から出口の最短経路を求める（完全迷路なら経路は一意）。

**確認:** 全セル連結、共有壁整合、辺数が `width * height - 1`、入口から出口の経路が一つであること。出力後に `maze_analyzer.py` でも検査すること。

## 5. `42` パターンを設計する

`42` を「通路から切り離した、完全に閉じたセル群」として扱い、表示専用の座標集合として管理します。

1. 最小対応サイズと、パターンのセル配置を定数化する。
2. 入口・出口・四隅・中央を避ける位置を探索する。
3. パターンセルの4壁を閉じ、周囲セル側にも対応する壁を閉じる。
4. 小さすぎて置けない場合、生成を継続しつつ標準エラー等へ明確なメッセージを出す。
5. 連結性の検証では `42` のセルを到達可能セルの対象から除外する。

**確認:** 通常サイズでは可視化に `42` が読め、小サイズではクラッシュせず警告のみになること。

## 6. 非完全（Pac-Man 風）モードを生成する

完全迷路を土台にし、制約を保ちながら追加の壁を開けます。

1. まず手順4の完全迷路を生成する。
2. `42` を除く候補壁をシャッフルして走査する。
3. 壁を開ける前に、3×3 の開放領域を新たに作らないかを局所判定する。
4. 追加壁を複数開け、少なくとも二つの独立したループを作る。
5. 四隅と中央を通路にする。必要なら、それらのセルを接続する壁を安全に開ける。
6. 行き止まり数を計測し、上限を設けて追加壁開放で減らす。理想は0。
7. 全通路の連結、ループ数、行き止まり数、禁止開放領域を検証する。満たせなければ、決まった試行回数までシード系列を変えて再生成し、失敗時は明確なエラーにする。

グラフとして、通路セル数を `V`、開放通路（辺）数を `E`、連結成分数を `C=1` とすると、独立ループ数は `E - V + C` です。`42` セルは `V` と検証対象から外します。非完全モードではこの値が2以上必要です。

**確認:** 複数シード・複数サイズで analyzer を回し、四隅・中央・連結性・ループ2本以上・3×3不在を自前テストでも確認すること。

## 7. 出力・解答経路を実装する

1. 各セルの壁ビットを大文字16進数1桁に変換する。
2. 上から下、左から右の順に1行ずつ出力する。
3. 空行のあとに、入口、出口、最短経路をそれぞれ1行で書く。
4. `with open(..., encoding="utf-8", newline="\n")` を使い、すべての行を `\n` で終える。
5. 出力先の書き込み失敗も捕捉して、わかりやすく失敗させる。

**確認:** 出力を再読込して寸法・各16進桁・メタデータ・共有壁を検証し、`maze_analyzer.py` に渡すこと。

## 8. 可視化と操作を実装する

最短ルートは **ASCII 描画**です。まず必須機能を端末で完成させ、余力があれば MLX を追加します。

1. 壁を `+---+` / `|` 形式で描画する。
2. 入口を `E`、出口を `X`、最短経路を `.` などで表示し、優先順位を決める。
3. `r`: 再生成、`p`: 解答経路の表示／非表示、`c`: 壁色変更、`q`: 終了、などを実装する。
4. ANSI カラー利用時は非対応端末でも読める表示にフォールバックする。
5. `42` は閉鎖セルの塊として色または記号で判別可能にする。

**確認:** 必須の三操作が機能し、解答経路が壁を横切らず入口から出口へ到達すること。

## 9. CLI 統合と失敗設計

`a_maze_ing.py` はオーケストレーターだけにします。

1. 引数個数を確認する。
2. 設定を読む。
3. 生成器を作って対象モードを生成する。
4. 生成物を内部検証する。
5. ファイルへ保存する。
6. 可視化を起動する。
7. 予期可能な例外は標準エラーへ1行で説明し、非ゼロ終了する。

ライブラリ層は `print()` や `sys.exit()` を呼ばず、例外または戻り値で失敗を返します。CLI 層だけが表示と終了コードを担当します。

## 10. パッケージ化・文書化

1. `pyproject.toml` に `mazegen` のビルド設定、バージョン、Python要件、ライセンスを定義する。
2. `MazeGenerator` を `mazegen/__init__.py` から公開する。
3. `python -m build` で `.whl` または `.tar.gz` を生成し、要求どおりリポジトリ直下に `mazegen-*` を配置する。
4. 新規仮想環境で成果物をインストールし、短い利用例を実行する。
5. 再利用・配布を許可する `LICENSE.md`（例: MIT License）を作る。
6. README に仕様、アルゴリズム選定理由、利用例、AI利用、チーム運営を記載する。

## 11. 最終チェックリスト

- [ ] `make install`、`make run`、`make debug`、`make clean`、`make lint` が動く。
- [ ] `a_maze_ing.py` という名前で、引数は設定ファイル1個だけである。
- [ ] エラー条件でクラッシュせず、明確なメッセージと非ゼロ終了になる。
- [ ] 同一 `SEED` で同一の迷路になる。
- [ ] 共有壁・外周壁・入口／出口・最短経路が整合する。
- [ ] `PERFECT=True` は完全迷路、`PERFECT=False` はプレイ可能なループ付き盤面になる。
- [ ] `42` が可能なサイズでは表示され、不可能なサイズでは通知される。
- [ ] 可視化で再生成・経路表示切替・壁色変更ができる。
- [ ] `maze_analyzer.py`、pytest、flake8、mypy を通過する。
- [ ] パッケージをソースからビルドし、別環境でインストールして使える。
- [ ] README と LICENSE.md が要件を満たす。

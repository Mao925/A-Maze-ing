# A-Maze-ing 2人用 実装分担表

この分担は、共有のデータモデルを先に合意し、依存関係が少ない作業を並行化する前提です。担当者名は開始時に `担当者A` / `担当者B` を実名またはログイン名に置き換えてください。

## 担当範囲

| 区分 | 担当者A: 生成・検証 | 担当者B: 入出力・表示・配布 |
| --- | --- | --- |
| 基盤 | `MazeGenerator`、セル壁モデル、方向・ビット定義 | プロジェクト雛形、Makefile、開発依存、`.gitignore` |
| 設定 | 設定値の意味・制約を定義 | 設定ファイルのパース、型変換、エラー文言、`config.txt` |
| 生成 | DFS完全迷路、シード、非完全モード、`42`配置 | 生成器を呼ぶ CLI の流れと終了コード |
| 解法・検証 | BFS最短経路、連結性、壁整合、ループ数、3×3禁止、行き止まり計測 | 出力ファイルの直列化／再読込テスト、`maze_analyzer.py` 実行補助 |
| 表示 | 描画用に迷路・経路・`42`の読み取りAPIを提供 | ASCII表示、再生成、経路表示切替、色変更 |
| 配布・文書 | `mazegen` 公開API、docstring、利用例の技術確認 | `pyproject.toml`、ビルド、LICENSE、README編集 |
| テスト | 生成器・グラフ検証の単体テスト | パーサー・出力・CLI・描画のテスト |

## 作業順と受け渡し

| フェーズ | 担当者A | 担当者B | 完了条件 |
| --- | --- | --- | --- |
| 0. 合意（最初に共同） | 座標系、壁ビット、内部データモデルを決める | 同左。設定値とCLIの責務を決める | 下記「共有契約」を文書化してレビュー済み |
| 1. 土台（並行） | `MazeGenerator` の空実装と型を作る | Makefile、依存関係、ディレクトリ、`config.txt` を作る | `make lint` と空テストが実行可能 |
| 2. 最小機能（並行後に統合） | 完全迷路、BFS、壁整合検証を実装 | パーサー、16進出力、CLIを実装 | 固定設定で完全迷路を出力可能 |
| 3. 必須拡張（並行） | `42`、非完全モード、ループ／3×3／行き止まり検証 | ASCII描画と `r` / `p` / `c` 操作を実装 | 両モードの迷路が表示・検証可能 |
| 4. 仕上げ（共同） | 生成器テスト、シード別のプロパティテスト | パッケージビルド、README、LICENSE、統合テスト | 最終チェックリストを全て満たす |

## 共有契約（フェーズ0で固定）

この契約を先に固定すれば、両者は同じ内部実装を触らずに進められます。

```python
Point = tuple[int, int]  # (x, y): 左上が (0, 0)、x は右、y は下へ増加

class MazeGenerator:
    def __init__(self, width: int, height: int, seed: int | None = None) -> None: ...
    def generate(self, perfect: bool) -> None: ...
    def shortest_path(self, start: Point, goal: Point) -> list[str]: ...
    def wall_mask(self, point: Point) -> int: ...  # N=1, E=2, S=4, W=8
    @property
    def blocked_cells(self) -> frozenset[Point]: ...  # `42` を構成する完全閉鎖セル
```

- 座標は常に `(x, y)`、原点は左上とする。
- 壁は `N=1`、`E=2`、`S=4`、`W=8` とする。
- `wall_mask()` は出力に使う壁ビット値を返す。描画・出力側は内部のリスト構造に直接アクセスしない。
- `shortest_path()` は入口を含まない移動列（例: `['E', 'E', 'S']`）を返す。
- `blocked_cells` のセルは通常の連結性・ループ計算の対象外である。
- ライブラリは `print()` / `sys.exit()` をしない。利用者への文言と終了コードはCLI側（担当者B）が担当する。

## マイルストーン別のレビュー担当

| 時点 | 担当者Aが確認するもの | 担当者Bが確認するもの |
| --- | --- | --- |
| 完全迷路の初回統合 | Bの出力が壁ビットと経路契約に合うか | Aの生成結果が analyzer と最短経路で正しいか |
| 非完全モード統合 | Bの描画が壁・`42`・経路を正しく反映するか | Aのループ数、四隅・中央、行き止まり、3×3制約が正しいか |
| 配布前 | READMEのアルゴリズム説明とAPI例が実装に一致するか | パッケージのクリーン環境インストールとMakefileが動くか |
| 提出前 | A/B双方が相手担当の主要関数を説明・小修正できるか | 同左 |

## 共有で守るルール

- 同じファイルを同時に大きく編集しない。`generator.py` は主にA、`a_maze_ing.py`・表示・出力は主にBが編集する。
- 共有契約を変える必要が出たら、実装前に相談し、この表と型定義を同時に更新する。
- 小さい単位でコミットし、統合前に `pytest`、`make lint`、対象設定での `maze_analyzer.py` を実行する。
- 毎日短時間で、詰まり・仕様判断・次の受け渡し物を同期する。
- README の「チームとプロジェクト管理」節には、実際の役割分担、計画の変化、良かった点、改善点、使用ツールを記録する。

## 統合時の受け入れチェック

- [ ] 担当者Bは `MazeGenerator` の公開APIだけで出力・描画でき、内部表現に依存していない。
- [ ] 担当者Aの生成器は `PERFECT=True` と `False` の双方で、同一シードの再現性を満たす。
- [ ] 設定不正、出力失敗、小サイズの `42` 不能時に、未処理例外が出ない。
- [ ] 完全モードと非完全モードの各設定を `maze_analyzer.py` で検査した。
- [ ] 2人とも、相手の主要担当を簡単に説明・修正できる。

# `mazegen` 実装設計解説 — 初心者向け

このドキュメントは、`mazegen` が**どのような考え方で迷路を作っているのか**を、Pythonやアルゴリズムを学び始めた人でも追えるように説明したものです。

細かい実装を読む前に、まず全体像だけ掴むと理解しやすくなります。

`mazegen` がやっていることは、大きく分けると次の3つです。

1. 迷路を作る
2. 作った迷路がルールを守っているか確認する
3. 入口から出口までの最短経路を探す

迷路生成の中心となるクラスが `MazeGenerator` です。

---

## 1. `mazegen` は何のためのパッケージか

`mazegen` は、**迷路そのものを作る処理だけを担当するライブラリ**です。

ここがかなり重要です。

たとえば迷路を使うアプリケーションには、次のような仕事があります。

- 設定ファイルを読む
- コマンドライン引数を受け取る
- 迷路を画面に表示する
- MLXで描画する
- ファイルへ保存する
- エラーをユーザー向けに表示する

しかし、`mazegen` はこれらを担当しません。

`mazegen` の仕事は、あくまで

> 「指定された大きさの迷路を作り、その構造をPythonから利用できる状態で返す」

ことです。

そのため、基本的には次のように使います。

```python
from mazegen import MazeGenerator

maze = MazeGenerator(width=20, height=15, seed=42)
maze.generate()
```

そして生成後に、

```python
maze.wall_mask((3, 2))
maze.blocked_cells
maze.shortest_path((0, 0), (19, 14))
```

のように迷路の情報を取得します。

### なぜ表示処理と迷路生成を分けるのか

もし迷路生成コードの中に `print()` やMLX描画まで全部書いてしまうと、あとから

- GUIで表示したい
- Webアプリで使いたい
- テストだけしたい
- 別プロジェクトから `import` したい

となったときに再利用しにくくなります。

そこで、

```text
迷路を作る部分
    ↓
mazegen

迷路を表示・保存する部分
    ↓
外側のアプリケーション
```

という役割分担にしています。

ライブラリ内部では、基本的に `print()` や `sys.exit()` を使いません。

問題が起きた場合は、呼び出し側が扱えるように

- `ValueError`
- `MazeGenerationError`
- `RuntimeWarning`

などで通知します。

---

# 2. 最初に知っておきたい5つの考え方

この設計を読むうえで、次の5つを理解しておくとかなり楽です。

### 1. セル

迷路を構成する1マスです。

```text
+---+
|   |
+---+
```

### 2. 壁

各セルには

- 北 `N`
- 東 `E`
- 南 `S`
- 西 `W`

の4方向の壁があります。

### 3. グラフ

迷路を、

- セル = 点
- 通路 = 点と点を結ぶ線

として考える方法です。

たとえば、

```text
A -- B -- C
     |
     D
```

という迷路があれば、A・B・C・Dがセル、線が通路です。

迷路生成アルゴリズムでは、この「グラフとして考える」視点が非常に重要です。

### 4. 連結

どの通常セルから出発しても、通路をたどれば他の通常セルへ到達できる状態です。

迷路なのに一部の場所へ絶対に行けない場合、それは連結ではありません。

### 5. ループ

同じ場所へ別ルートから戻ってこられる構造です。

```text
A -- B
|    |
D -- C
```

この四角形にはループがあります。

完全迷路ではループを作りません。

Pac-Man風の非完全迷路では、逆にいくつかループを作ります。

---

# 3. ファイル構成と公開API

`mazegen` パッケージは主に次の2ファイルで構成されています。

## `__init__.py`

利用者が直接触る「入口」です。

ここから次のものを公開します。

- `MazeGenerator`
- `Point`
- `Direction`
- `MazeGenerationError`

つまり利用者は、内部ファイルを細かく意識せず

```python
from mazegen import MazeGenerator
```

と書けます。

## `generator.py`

実際の迷路生成処理が入っている中心ファイルです。

ここには、

- 迷路データ
- 壁操作
- DFSによる迷路生成
- BFSによる最短経路探索
- `42` パターン配置
- 迷路の検証

などがあります。

---

## 基本的な使い方

```python
from mazegen import MazeGenerator

maze = MazeGenerator(width=20, height=15, seed=42)

entry = (0, 0)
exit_ = (19, 14)

maze.generate(
    perfect=False,
    protected_cells=frozenset({entry, exit_}),
)

cell_walls = maze.wall_mask((3, 2))
closed_42_cells = maze.blocked_cells
solution = maze.shortest_path(entry, exit_)
```

それぞれ順番に見ると、

```python
MazeGenerator(width=20, height=15, seed=42)
```

は20×15の迷路生成器を作っています。

`seed=42` は乱数の種です。

同じseedを指定すると、基本的に同じランダム結果を再現できます。

```python
perfect=False
```

は、完全迷路ではなく、ループを持つPac-Man風迷路を作る指定です。

```python
protected_cells=frozenset({entry, exit_})
```

は、入口・出口などを `42` の閉鎖セルとして使わないよう保護しています。

範囲外の座標を指定すると、生成前に `ValueError` になります。

---

# 4. 座標は `(x, y)`

迷路では左上を `(0, 0)` としています。

```text
(0,0) (1,0) (2,0)
(0,1) (1,1) (2,1)
(0,2) (1,2) (2,2)
```

右へ進むと `x` が増えます。

下へ進むと `y` が増えます。

そのため方向ごとの差分は次のようになります。

| 方向 | 座標差 | 意味 |
| --- | --- | --- |
| `N` | `(0, -1)` | 1マス上 |
| `E` | `(1, 0)` | 1マス右 |
| `S` | `(0, 1)` | 1マス下 |
| `W` | `(-1, 0)` | 1マス左 |

---

# 5. 1つの整数で4方向の壁を表す

ここは初見だと一番わかりにくい部分です。

各セルは、4方向の壁を4つの別々の変数で持つのではなく、**1つの整数のビット**で表しています。

| 方向 | ビット | 10進数 |
| --- | ---: | ---: |
| `N` | `0b0001` | 1 |
| `E` | `0b0010` | 2 |
| `S` | `0b0100` | 4 |
| `W` | `0b1000` | 8 |

たとえば、

```text
0b1111
```

なら、4つのビットが全部 `1` です。

つまり、

```text
北壁あり
東壁あり
南壁あり
西壁あり
```

という意味になります。

10進数では `15`、16進数では `F` です。

---

## 例：北と西だけ壁がある

```text
N = 0001
W = 1000
---------
    1001
```

なので壁値は

```python
0b1001
```

になります。

10進数なら `9` です。

---

## なぜビットで管理するのか

迷路の出力形式自体が壁をビットで扱うため、内部でも同じ形式を使うと変換が少なくて済みます。

さらに、壁があるかどうかをビット演算で簡単に確認できます。

イメージとしては、

```python
walls & Direction.N
```

が0でなければ「北壁あり」です。

壁を消すときは、その方向のビットだけを0にします。

---

# 6. `_grid[y][x]` が1セル分の壁情報

内部では迷路全体を

```python
list[list[int]]
```

として持ちます。

つまり2次元リストです。

```python
_grid[y][x]
```

で `(x, y)` のセルを取得します。

注意点は、座標は `(x, y)` なのに、Pythonの2次元リストでは

```python
_grid[y][x]
```

と `y` が先になることです。

たとえば `(3, 2)` のセルなら、

```python
_grid[2][3]
```

です。

初期状態ではすべてのセルを

```python
0b1111
```

にします。

つまり、最初は

> 「全部の壁が閉じている巨大なマス目」

を作ります。

そこから必要な壁だけを壊して通路を作っていきます。

---

# 7. 壁は必ず両側を同時に更新する

たとえばセルAの東側にセルBがあるとします。

```text
A | B
```

Aの東壁とBの西壁は、物理的には同じ1枚の壁です。

そのため、A側だけ壁を消してB側の壁を残すと、

```text
AからBへは行ける
BからAへは行けない
```

という壊れた迷路になってしまいます。

そこで壁更新は

- `_remove_wall()`
- `_add_wall()`

にまとめています。

Aの東壁を消すなら、必ずBの西壁も同時に消します。

```text
A の E を削除
    ↓
B の W も削除
```

このように、**間違えやすい処理を1か所に集約する**のが設計上のポイントです。

---

# 8. `generate()` の全体の流れ

迷路生成は、一発で適当に壁を壊して終わりではありません。

次のような流れで作ります。

```text
初期化
  ↓
42を配置
  ↓
DFSで基本迷路を作る
  ↓
必要ならループを追加
  ↓
行き止まりを減らす
  ↓
完成した迷路を検証
  ↓
OKなら採用
```

実際には最大20回まで試します。

1. 全セルを壁で閉じる
2. 配置できる場合は `42` の閉鎖セルを置く
3. DFSで通常セルをすべてつなぐ
4. 非完全モードならループを作る
5. 非完全モードなら行き止まりを減らす
6. 完成した迷路を検証する
7. 条件を満たせば終了する

ランダム生成なので、一度の生成で必ずすべての制約を満たせるとは限りません。

そのため

> 作る → 検査する → ダメなら作り直す

という設計になっています。

---

# 9. seedと再現性

各生成試行では、

```text
seed + attempt
```

を元に専用の `random.Random` を作ります。

たとえば `seed=42` なら、

```text
1回目: 42
2回目: 43
3回目: 44
...
```

のようなイメージです。

これにより、Pythonプログラムの他の場所で乱数を使っていても、迷路生成結果が影響されにくくなります。

つまり、固定seedなら再現性があります。

一方 `seed=None` の場合は、生成ごとに新しいランダム値を使うため、再生成すると違う迷路を得られます。

20回すべて失敗した場合は、

```python
MazeGenerationError
```

を送出します。

その際、最後にどの検証条件で失敗したかも含めます。

---

# 10. `42` パターンとは何か

A-Maze-ingでは、迷路の中に `42` の形をした閉鎖領域を作れるようにしています。

`FORTY_TWO_PATTERN` では、

```text
# = 閉鎖セル
. = 通常セル
```

として5行7列のパターンを定義しています。

閉鎖セルは、普通の迷路セルとは違い、**四方すべての壁が閉じたまま**です。

つまり中へ入ることはできません。

---

## なぜ配置場所を慎重に選ぶのか

適当に `42` を置くと、迷路を真っ二つにしてしまう可能性があります。

たとえば、細い迷路の中央を閉鎖セルで完全に塞いでしまうと、左側と右側を行き来できなくなります。

そこで配置候補は次の条件を満たす必要があります。

- 外周から1セル以上離れている
- `protected_cells` と重ならない
- 四隅と重ならない
- 中央候補と重ならない
- `42` を除いた残りのセルが連結している

最後の

> `42` を除いた残りが連結している

という条件が特に重要です。

これは、まだ壁を作る前の段階で

> 「そもそも `42` の置き方のせいで移動不能になっていないか」

を確認しています。

小さい迷路などで `42` を安全に置けない場合は、迷路生成自体は続行します。

その代わり

```python
RuntimeWarning
```

を出して、配置できなかったことを知らせます。

---

# 11. 完全迷路とは何か

完全迷路（perfect maze）とは、簡単に言うと

> どの2セルの間にも、経路がちょうど1つしか存在しない迷路

です。

つまりループがありません。

```text
A -- B -- C
     |
     D
```

この構造なら、AからDへ行く方法は1通りです。

一方、

```text
A -- B
|    |
D -- C
```

ではAからCへ行くルートが複数あります。

これは完全迷路ではありません。

---

# 12. DFSで完全迷路を作る

完全迷路の生成にはDFS（Depth First Search、深さ優先探索）を使います。

迷路生成でのDFSは、かなり直感的です。

イメージとしては

> 「行けるところまでひたすら進み、行き止まりになったら少し戻って別方向を探す」

という動きです。

たとえば、

```text
開始
 ↓
右へ進む
 ↓
さらに右へ進む
 ↓
行けない
 ↓
1つ戻る
 ↓
下へ進む
```

という感じです。

---

## `_generate_spanning_tree()` の処理

1. まだ訪問していない隣接セルを探す
2. そのセルとの間の壁を壊す
3. そのセルへ移動する
4. 行ける未訪問セルがなくなったら戻る
5. すべての通常セルを訪問するまで続ける

この「戻る場所」を覚えるためにスタックを使います。

```python
stack = []
```

再帰関数でもDFSは書けますが、この実装では明示的なリストをスタックとして使います。

理由は、大きな迷路でもPythonの再帰回数上限に引っかかりにくくするためです。

---

# 13. 全域木とは何か

`_generate_spanning_tree()` という名前に出てくる「全域木」は、初心者にはかなり難しい言葉です。

分解すると、

- 全域 = すべてのセルを含む
- 木 = ループのない連結グラフ

です。

つまり全域木とは、

> 「すべての通常セルをつないでいるが、ループは1つもない構造」

です。

通常セル数を `V` とすると、全域木の通路数 `E` は必ず

```text
E = V - 1
```

になります。

たとえば通常セルが10個なら、全域木の通路は9本です。

DFSでは、**未訪問セルへ進むときだけ壁を開く**ため、すでにつながっている場所同士を余計につなぎません。

その結果、ループのない迷路になります。

---

# 14. 非完全迷路は「完全迷路を壊して」作る

Pac-Man風の非完全迷路では、最初から複雑なループ迷路を直接作るのではありません。

まずDFSで完全迷路を作ります。

```text
必ず全部つながった迷路
```

を最初に作るわけです。

そのあとで追加の壁を壊してループを作ります。

```text
完全迷路
   ↓
壁を追加で開く
   ↓
ループが増える
```

この方法の利点は、最初の時点ですでに全セルの連結性が保証されていることです。

---

# 15. ループ数 `E - V + 1`

連結グラフの独立したループ数は、

```text
loops = E - V + 1
```

で求められます。

ここで、

- `V` = 通常セル数
- `E` = 開いている共有壁、つまり通路の数

です。

全域木では

```text
E = V - 1
```

なので、

```text
loops
= (V - 1) - V + 1
= 0
```

となります。

そこから壁を1枚追加で開くと、通路 `E` が1本増えるため、ループ数も1増えます。

非完全モードでは `_add_loops()` が壁を追加で開き、独立ループ数が2以上になることを目指します。

単純に

> 「壁を2枚壊したからループ2個」

と考えず、完成したグラフからループ数を計算しているのがポイントです。

---

# 16. 行き止まりとは何か

あるセルから出られる通路が1本しかない場合、そのセルは行き止まりです。

グラフ用語では、そのセルの「次数」が1です。

```text
A -- B -- C
```

Aから出る道がB方向しかないなら、Aの次数は1です。

非完全迷路では、Pac-Man風にするために行き止まりを減らします。

`_reduce_dead_ends()` は、行き止まりセルの周囲を見て、安全に開けられる壁があれば追加で壁を開きます。

```text
行き止まり
   ↓
別方向の壁を開く
   ↓
新しい逃げ道ができる
```

ただし、何でも開けばよいわけではありません。

壁を開いた結果、禁止されている3×3完全開放領域ができた場合は、その変更を取り消します。

---

# 17. 変更してから検査し、ダメなら戻す

ループ追加や行き止まり削減では、次のような考え方を使っています。

```text
壁を開く
  ↓
ルール違反が起きたか確認
  ↓
OK → そのまま
NG → 壁を元に戻す
```

データベースの「トランザクション」に少し似た考え方です。

複雑な条件を全部事前計算して

> 「この壁を開けても絶対大丈夫か？」

と予測するより、

> 「実際に開けて完成状態を検査し、ダメなら戻す」

ほうが実装が分かりやすく、境界や `42` 周辺でも同じルールを使えます。

---

# 18. 3×3完全開放領域を禁止する

A-Maze-ingの要件では、3×3の9セルが完全につながった広場のような領域を作らないようにします。

3×3セルを考えると、内部には

- 横方向の共有壁が6本
- 縦方向の共有壁が6本

あります。

合計12本です。

```text
□ □ □
□ □ □
□ □ □
```

この内部12本がすべて開いていると、3×3全体が完全に開放されています。

`_has_open_3x3()` はすべての3×3範囲を調べて、これが存在しないか確認します。

重要なのは、**3×3だけを禁止している**ことです。

そのため、

- 2×3
- 3×2

程度の広い場所は残せます。

---

# 19. 最短経路探索にはBFSを使う

`shortest_path()` はBFS（Breadth First Search、幅優先探索）を使います。

DFSが

> 「1方向へ深く進む」

探索なのに対し、BFSは

> 「現在地から近い場所を順番に広げる」

探索です。

イメージとしては、水面に石を落としたときの波紋に近いです。

```text
距離0: スタート
距離1: スタートの隣
距離2: さらにその隣
距離3: さらにその隣
...
```

この順番で探索します。

迷路では1マス移動するコストがすべて同じ `1` なので、最初にゴールへ到達した経路が最短になります。

---

# 20. BFSで経路そのものを復元する方法

BFSでゴールを見つけただけでは、

> 「何手で着いたか」

は分かっても、

> 「具体的にN→E→E→Sのようにどう進んだか」

をまだ復元する必要があります。

そこで各セルについて、

- どのセルから来たか
- どの方向へ移動して来たか

を記録します。

たとえば、

```text
A → B → C → Goal
```

なら、Goalから親を逆にたどって

```text
Goal ← C ← B ← A
```

と戻り、最後に順序を反転します。

その結果、

```python
["E", "E", "S", ...]
```

のような最短経路を得られます。

開始点と終了点が同じ場合は、移動不要なので空リストです。

```python
[]
```

範囲外座標、閉鎖セル、経路なしなどの場合は `ValueError` になります。

---

# 21. `_passages()` を通して移動可能方向を調べる

最短経路探索では、単に隣接セルを見るだけではありません。

壁が開いていて実際に移動できる方向だけを `_passages()` から取得します。

これにより、

- 壁を突き抜ける
- `42` の閉鎖セルへ入る
- 壁情報の不整合を無視する

といった問題を防げます。

「移動可能かどうか」の判定も1か所に集約しているわけです。

---

# 22. 生成と検証を分ける理由

この設計では、迷路生成アルゴリズムだけを信用しません。

つまり、

> 「DFSで作ったんだから正しいはず」

とは考えません。

最後に `_validation_errors()` で、完成した迷路そのものを改めて検査します。

これは非常に大事な設計です。

たとえば将来、迷路生成方法をDFS以外へ変更したとしても、同じ検証処理を使えます。

```text
生成アルゴリズムA ─┐
生成アルゴリズムB ─┼→ 共通の検証処理
生成アルゴリズムC ─┘
```

検証処理が品質ゲートになります。

---

# 23. `_validation_errors()` が確認すること

検査項目は次のとおりです。

### グリッドそのもの

- 幅・高さが正しいか
- 壁値が `0..15` に収まっているか

4ビットで壁を表すため、有効な値は0〜15です。

### 外周

迷路の外へ出られないように、外周の壁が閉じているか確認します。

### 共有壁

隣接セル同士で壁情報が一致しているか確認します。

```text
左セルのE壁 == 右セルのW壁
```

でなければ壊れた迷路です。

### `42`

- `42` セルが4壁すべて閉じているか
- `protected_cells` と重なっていないか

### 連結性

`42` 以外の通常セルすべてに到達できるか確認します。

### 3×3領域

完全開放された3×3領域がないか確認します。

### 完全迷路

ループ数が0か確認します。

### 非完全迷路

- ループ数が2以上か
- 解消可能な行き止まりが2以下か
- 四隅や中央候補が通常セルとして使えるか

を確認します。

`_validation_errors()` は最初のエラー1個だけで止まらず、見つかった問題をまとめて返します。

そのため、失敗原因を調査しやすくなっています。

---

# 24. 「連結性検査」はどう考えればよいか

連結性検査では、通常セルのどこか1つから探索を開始します。

そこから壁を通って移動できるセルをすべて訪問します。

最後に、

```text
訪問できた通常セル数
```

と

```text
通常セルの総数
```

が同じなら、すべてつながっています。

イメージとしては、迷路のある地点からインクを流し込んで、通常セル全部にインクが届くか確認するようなものです。

---

# 25. 計算量 `O(V)` とは何か

セル数を

```text
V = width * height
```

とします。

たとえば20×15なら、

```text
V = 300
```

です。

DFS、BFS、連結性検査などは、基本的に各セルを数回見る程度なので

```text
O(V)
```

です。

これは、セル数が2倍になると処理量もだいたい2倍になる、というイメージです。

3×3検査も、各3×3窓について確認する壁の本数は最大12本で固定なので、全体ではおおむね `O(V)` です。

---

# 26. なぜ一部は `O(V^2)` になるのか

非完全迷路では、候補となる壁を1枚開けるたびに

```text
3×3完全開放領域ができなかったか
```

を迷路全体で検査することがあります。

候補壁が `O(V)` 個あり、そのたびに `O(V)` の検査を行えば、最悪では

```text
O(V) × O(V) = O(V^2)
```

になります。

ただしA-Maze-ingの通常サイズでは、ここでは極端な高速化よりも

- 実装が読みやすい
- 正しく検証できる
- バグを減らせる

ことを優先しています。

---

# 27. `_` で始まるメソッドの意味

Pythonでは、

```python
_remove_wall()
_generate_spanning_tree()
_validation_errors()
```

のように `_` から始まる名前は、

> 「これは基本的にクラス内部で使うものです」

という慣習を表します。

一方、外部利用者には

```python
generate()
wall_mask()
shortest_path()
```

などの公開APIを使ってもらいます。

利用者が `_grid` などの内部構造へ直接依存しないようにすることで、将来内部実装を変更しても利用側のコードを壊しにくくなります。

---

# 28. この設計を一言でまとめると

`mazegen` の基本思想は、

> **まず安全な骨格を作り、そのあと必要な複雑さを足し、最後に完成品そのものを検査する**

ことです。

完全迷路では、

```text
全壁閉鎖
  ↓
DFSで全域木を作る
  ↓
検証
```

非完全迷路では、

```text
全壁閉鎖
  ↓
DFSで全域木を作る
  ↓
ループを追加
  ↓
行き止まりを減らす
  ↓
3×3違反などをチェック
  ↓
検証
```

となります。

最短経路は生成処理とは別に、BFSで求めます。

---

# 29. コードを読むおすすめ順序

初心者が `generator.py` を上から全部読もうとすると、かなり混乱しやすいです。

次の順番で読むのがおすすめです。

1. `MazeGenerator.__init__()`
2. `generate()`
3. `_remove_wall()` / `_add_wall()`
4. `_generate_spanning_tree()`
5. `_add_loops()`
6. `_reduce_dead_ends()`
7. `_has_open_3x3()`
8. `_validation_errors()`
9. `shortest_path()`

まず `generate()` で全体の流れを掴み、そのあと内部メソッドへ降りていくと理解しやすくなります。

---

# 30. 用語集

| 用語 | 意味 |
| --- | --- |
| セル | 迷路の1マス |
| 壁マスク | 4方向の壁をビットで表した整数 |
| ビット | `0` / `1` で情報を表す最小単位 |
| グラフ | セルと通路を「点と線」として扱う考え方 |
| 頂点 `V` | グラフ上の点。ここでは通常セル |
| 辺 `E` | グラフ上の線。ここでは通路 |
| 連結 | すべての通常セル同士が到達可能な状態 |
| DFS | 1方向へ深く進み、行き止まりで戻る探索 |
| BFS | 近い場所から順番に広げる探索 |
| スタック | 後から入れたものを先に取り出すデータ構造 |
| 全域木 | 全セルをつなぎ、ループを持たない構造 |
| 完全迷路 | 任意の2セル間の経路が1つだけの迷路 |
| ループ | 複数ルートで同じ場所へ戻れる構造 |
| 次数 | あるセルから伸びている通路数 |
| 行き止まり | 次数が1のセル |
| seed | ランダム結果を再現するための値 |
| 不変条件 | 迷路が常に満たすべきルール |
| 公開API | 外部コードから使ってよいメソッドや型 |

---

# 31. 最低限ここまで理解できればOK

最初からすべての数式やビット演算を完全に理解する必要はありません。

まずは次の流れが説明できれば、設計の中心は掴めています。

```text
1. 最初は全部の壁を閉じる
2. 42を置けるなら置く
3. DFSで全セルをつなぐ
4. 完全迷路ならそこで基本形が完成
5. 非完全迷路なら壁を追加で開いてループを作る
6. 行き止まりを減らす
7. 3×3開放などのルール違反がないか検査する
8. 最短経路はBFSで探す
```

そして設計上もっとも重要なのは、

> **「生成アルゴリズムが正しいはず」と信じるのではなく、最後に完成状態を検証する**

という考え方です。

この考え方は迷路生成に限らず、実際のソフトウェア設計でも非常に重要です。

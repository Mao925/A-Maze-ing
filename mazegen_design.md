# `mazegen` 実装設計解説

## 1. 目的と責務

`mazegen` は、迷路の生成、構造検証、最短経路探索を再利用可能な形で提供するパッケージです。CLI、設定ファイル、ファイル出力、画面描画には依存しません。外側のアプリケーションは `MazeGenerator` を生成し、`generate()` を呼び、`wall_mask()`、`blocked_cells`、`shortest_path()` から結果を取得します。

この分離を選んだ理由は、PDF要件にある「後続プロジェクトから import できる単一クラス」を満たし、端末表示やMLX表示を変更しても迷路生成ロジックを変更せずに済むようにするためです。ライブラリ内では `print()` や `sys.exit()` を使わず、呼び出し側が扱える例外と警告で失敗を通知します。

## 2. ファイル構成と公開API

- `__init__.py`: 利用者向けの公開窓口です。`MazeGenerator`、座標型 `Point`、壁方向 `Direction`、生成失敗例外 `MazeGenerationError` を再公開します。
- `generator.py`: データモデル、生成アルゴリズム、BFS、構造検証を一つのモジュールに集約します。

基本的な利用方法は次のとおりです。

```python
from mazegen import MazeGenerator

maze = MazeGenerator(width=20, height=15, seed=42)
entry = (0, 0)
exit_ = (19, 14)
maze.generate(perfect=False, protected_cells=frozenset({entry, exit_}))

cell_walls = maze.wall_mask((3, 2))
closed_42_cells = maze.blocked_cells
solution = maze.shortest_path(entry, exit_)
```

`protected_cells` は入口・出口など、`42` の閉鎖セルと重ねてはいけない座標です。範囲外の保護座標は生成前に `ValueError` になります。

## 3. 壁データモデル

座標は左上を `(0, 0)` とする `(x, y)` のタプルです。各セルは一つの整数で4方向の壁を表します。

| 方向 | ビット | 座標差 | 反対方向 |
| --- | ---: | --- | --- |
| `N` | `0b0001` | `(0, -1)` | `S` |
| `E` | `0b0010` | `(1, 0)` | `W` |
| `S` | `0b0100` | `(0, 1)` | `N` |
| `W` | `0b1000` | `(-1, 0)` | `E` |

内部の `_grid` は `list[list[int]]` で、`_grid[y][x]` がセルの壁値です。初期状態は全セル `0b1111`、つまり4壁が閉じています。通路を作るときだけ `_remove_wall()` でビットを落とします。

壁更新を `_remove_wall()` と `_add_wall()` に集約したのは、共有壁の片側だけが変わる不整合を構造的に防ぐためです。たとえば東壁を開く操作は、必ず東隣セルの西壁も同時に開きます。出力形式と同じビット表現を内部でも使うことで、変換処理を減らし、`wall_mask()` の値をそのまま16進数化できます。

## 4. 生成パイプライン

`generate()` は、次の処理を最大20回まで試します。

1. 全セルを閉じた状態に戻す。
2. 配置可能なら閉鎖セルで `42` を配置する。
3. DFSで通常セルの全域木を作る。
4. 非完全モードなら、安全な壁を追加で開けてループを作り、解消可能な行き止まりを減らす。
5. 全不変条件をまとめて検証する。
6. 検証エラーがなければ結果を採用する。

各試行は `seed + attempt` の専用 `random.Random` を使います。この設計により、固定シードはプロセス全体の乱数状態に影響されず再現可能です。一方、シードが `None` の場合は生成呼び出しごとにシステム乱数から基準値を選ぶため、再生成操作で新しい迷路を得られます。

制約の組み合わせによって一度のランダム生成では条件を満たさない可能性があるため、生成後検証と決定的な再試行を組み合わせています。20回すべて失敗した場合は、最後の検証理由を含む `MazeGenerationError` を送出します。

## 5. `42` パターン

`FORTY_TWO_PATTERN` は `#` を閉鎖セル、`.` を通常セルとする5行7列の固定字形です。配置探索では次を満たす左上座標を上から順に選びます。

- 外周との間に1セルの余白がある。
- 入口・出口などの保護座標、四隅、中央候補と重ならない。
- パターンセルを除いた残りの盤面が、幾何学的に一つの連結成分である。

パターンセルは全域木の対象から外すため、4壁が閉じたまま残ります。通常セル側からそのセルへ壁を開くこともありません。小さい盤面などで配置できない場合は迷路生成自体を継続し、要件どおり `RuntimeWarning` で明示します。

連結性を壁生成前に確認する理由は、閉鎖セルの配置そのものが盤面を分断するケースを先に排除するためです。壁生成後に初めて分断を発見する方法より、失敗原因が明確になり、不要な生成試行も減ります。

## 6. 完全迷路の生成

`_generate_spanning_tree()` は反復版のDFS再帰バックトラッカーです。

- 未訪問の通常セルへ進むたびに、その間の共有壁を開く。
- 行き先がなくなったらスタックを戻る。
- 全通常セルを訪問するまで続ける。

訪問済みセルへ辺を追加しないため、作られるグラフは連結かつ閉路なしです。通常セル数を `V` とすると通路数は必ず `V - 1` となり、任意の二点間の経路が一つだけの完全迷路になります。

再帰ではなく明示的なリストのスタックを選んだ理由は、大きな迷路でもPythonの再帰上限に依存しないためです。DFSは長く曲がった通路を作りやすく、迷路らしい見た目を得やすいことも選定理由です。

## 7. 非完全（Pac-Man風）迷路

非完全モードも、まず連結性が保証された全域木を作ります。その後 `_add_loops()` が閉じた通常セル間の共有壁をランダム順に試し、独立ループ数が2以上になるまで通路を追加します。

連結グラフの独立ループ数は次式で求めます。

```text
loops = E - V + 1
```

`E` は開いた共有壁の本数、`V` は通常セル数です。単に「壁を2枚開けた」と判定せずグラフ量を使うため、将来生成手順を変更しても要件を正しく検査できます。

`_reduce_dead_ends()` は次数1のセルから、まだ閉じている通常セル方向の壁を開きます。ただし開放後に3x3領域が完全開放になる場合は `_add_wall()` で元に戻します。外周と `42` セルだけに囲まれ、追加の壁開放では解消できない行き止まりは、配布された analyzer と同じ基準で実数から除外します。

四隅と中央候補は `42` の配置対象から除外され、通常セル全体の連結性検証によって到達可能性が保証されます。

## 8. 3x3開放領域の防止

`_has_open_3x3()` は全3x3窓を走査し、内部の横6本、縦6本、合計12本の共有壁がすべて開いているかを調べます。12本すべてが開いている窓だけを禁止するため、要件で許可された2x3または3x2の領域は残せます。

ループ追加と行き止まり削減は、壁を一度開けてこの検査を行い、違反した場合だけ巻き戻すトランザクション型の処理です。複雑な事前予測より実際の完成状態を検査する方が、境界や `42` 周辺でも判定が一貫します。

## 9. 最短経路

`shortest_path()` は幅優先探索（BFS）を使用します。各到達セルについて「親セル」と「親から移動した方向」を記録し、ゴール到達後に逆順でたどって `N/E/S/W` の列を復元します。

全通路の移動コストが1なので、BFSが返す経路は必ず最短です。探索時は `_passages()` だけを使うため、片側の壁だけが開いた不整合や `42` セルを経路として採用しません。開始点と終了点の範囲外、閉鎖セル、経路なしはそれぞれ明確な `ValueError` になります。同一点間の経路は空リストです。

## 10. 検証戦略

`_validation_errors()` は最初のエラーで停止せず、見つかった問題を列挙します。検査対象は次のとおりです。

- グリッド寸法と壁値 `0..15`
- 外周4辺の閉鎖
- 隣接セル間の共有壁一致
- `42` セルの完全閉鎖と保護座標との非重複
- `42` 以外の全セルの連結性
- 完全開放3x3領域の不在
- 完全モードのループ数0
- 非完全モードのループ数2以上、解消可能な行き止まり2以下
- 非完全モードの四隅・中央が通常セルであること

生成処理と検証処理を分けた理由は、「アルゴリズムがそうなるはず」という仮定だけに依存せず、最終状態そのものを合否判定するためです。生成アルゴリズムを将来追加しても、同じ検証器を品質ゲートとして再利用できます。

## 11. 計算量と設計上の選択

セル数を `V = width * height` とすると、DFS、BFS、連結性検査、辺数・行き止まり計数は基本的に `O(V)` です。3x3検査も各窓を定数本の壁で調べるため `O(V)` です。非完全モードでは候補壁ごとに3x3全体を再検査するため最悪時は `O(V^2)` ですが、通常の課題サイズでは実装の明瞭さと検証の確実性を優先しています。

内部メソッドは `_` で始め、外部利用者が `_grid` や壁更新処理へ依存しないようにしています。これにより、内部表現や生成アルゴリズムを変更しても公開APIを維持できます。

# `tests` テスト設計解説

## 1. テストの目的

このテスト群は、実装の内部手順ではなく、PDF要件として観測できる結果を検証します。固定シードの例を一つ確認するだけでは、共有壁、連結性、ループ数などの構造的な欠陥を見逃すため、迷路をグラフとして独立に解析するヘルパーをテスト側にも用意しています。

テストは次の3ファイルに分かれます。

- `test_generator.py`: コンストラクタ、シード、公開アクセサ、最短経路、警告と例外など公開APIの振る舞い。
- `test_validation.py`: 完成した迷路の壁・グラフ・モード別不変条件。
- `conftest.py`: 分担用ディレクトリ配置で、リポジトリ直下の `mazegen` をテストから import 可能にする準備。

公開APIテストと構造検証テストを分けた理由は、失敗時に「利用方法の契約が壊れた」のか「生成物の数学的性質が壊れた」のかをすぐ判断できるようにするためです。

## 2. `test_generator.py` の設計

### 入力境界と失敗契約

幅・高さの0以下、範囲外座標、範囲外の保護座標をテストします。例外型だけでなくメッセージの要点も確認し、CLI層が利用者向けエラーへ変換できる情報を維持していることを保証します。

1セルの非完全迷路は、独立ループを2本作れないため `MazeGenerationError` になることを確認します。これは単なる異常入力ではなく「個々の値は有効だが、要件の組み合わせが実現不能」という境界です。

### シード再現性

同じサイズ・同じシード・同じモードで、全セルの壁スナップショットと `blocked_cells` が一致することを確認します。完全モードだけでなく、候補壁のシャッフルが増える非完全モードも対象です。別シードでは結果が異なることも検査し、乱数が固定値化されていないことを確認します。

スナップショットは `_grid` を直接参照せず、全セルに `wall_mask()` を呼んで構築します。テストが内部表現に結合すると、安全なリファクタリングまで妨げるためです。

### 最短経路

返された各 `N/E/S/W` を入口から順に適用し、次を確認します。

- 移動元の該当壁が開いている。
- 移動先の反対壁も開いている。
- `42` の閉鎖セルへ入らない。
- 最後に出口へ到達する。
- テスト側で独立に実装したBFS距離と同じ長さである。

到達だけでなく独立BFSと比較するのは、遠回りする有効経路を誤って「最短」と認めないためです。同一点間の空経路、範囲外点、`42` セルを端点にした場合も別に検査します。

### `42` 省略時の警告

3x3迷路は字形を配置できませんが、完全迷路自体は生成できます。`pytest.warns` で `RuntimeWarning` を確認することで、「小さい場合は省略可能だがコンソールへ通知する」という要件をテストします。

## 3. `test_validation.py` の設計

### テスト側グラフ解析

`normal_cells()`、`passages()`、`reachable()`、`graph_counts()` は、`wall_mask()` と `blocked_cells` だけから迷路を再解釈します。生成器の `_passages()` や `_loop_count()` をそのまま呼ぶと、実装とテストが同じバグを共有する可能性があるため、意図的に独立実装にしています。

`passages()` は移動元の壁と移動先の反対壁の両方を確認します。このため、共有壁が片側だけ開いた不正状態を通路として扱いません。

### 複数シードと両モード

共通不変条件は、完全・非完全の両モードと3種類のシードの組み合わせで検証します。固定された一つの迷路だけでは、DFSの分岐順やループ追加順に依存する欠陥が隠れるためです。

モード固有のテストでも、fixtureをシードでパラメータ化しています。テスト時間を短く保ちながらランダム分岐の幅を確保するため、代表値として `0`、`7`、`42` を選んでいます。

### 壁表現と共有壁

全セルについて壁値が `0x0..0xF` に収まること、外周の対応壁が閉じること、全隣接ペアで共有壁の開閉が一致することを確認します。これはファイルへ16進数1桁で出力できることと、壁をすり抜けないことの両方を保証します。

### `42` と連結性

通常セル集合を一つのセルからBFSし、到達集合が通常セル全体と一致することを確認します。到達不能を許すのは `blocked_cells` だけで、それらがすべて `0xF` であることも検査します。

さらに、閉鎖セル集合を左上基準に正規化し、5x7の `42` 字形そのものと比較します。単に「閉鎖セルが20個ある」だけでは別の図形でも合格してしまうため、見た目の要件を座標集合として検証しています。

### 完全迷路

通常セル数 `V` と開いた共有壁数 `E` を独立に数え、次を確認します。

```text
E = V - 1
loops = E - V + 1 = 0
```

別テストで連結性も確認しているため、この2条件から通常セル部分が全域木であり、任意の二点間に経路が一つだけであることが分かります。

### 非完全迷路

`loops >= 2` を検査し、完全迷路から壁を一枚だけ外した盤面を除外します。行き止まりは次数1のうち、閉じた壁の向こうに通常セルがあり、追加開放で解消できるものを数えます。この定義は配布された `maze_analyzer.py` の「real dead-end」と同じで、`42` と外周に囲まれた不可避な端を分けます。

四隅と中央が通常セルであり、少なくとも一つの通路を持つことも確認します。全通常セルの連結性と合わせて、これらがプレイ可能領域に属することを保証します。

### 3x3開放領域

全3x3窓について、内部の横6本と縦6本の壁を調べます。12本がすべて開いた窓が一つもないことを確認します。2x3や3x2まで禁止する過剰なテストにはせず、PDFの境界をそのまま符号化しています。

## 4. `conftest.py` の役割

現在の分担用構成では実装がリポジトリ直下の `mazegen/`、テストが `submit/tests/` にあります。pytestは設定ファイルの位置から `submit/` をルートとして選ぶ場合があるため、`conftest.py` がリポジトリ直下を `sys.path` の先頭へ一度だけ追加します。

この処理はテスト実行環境だけに限定され、製品コードのimport動作は変更しません。実装を `submit/` へ統合した後は、パッケージを通常インストールする構成へ変更し、この補助を削除できます。

## 5. 実行と品質ゲート

リポジトリ直下から次を実行します。

```bash
pytest -q
flake8 mazegen submit/tests
mypy mazegen submit/tests \
  --warn-return-any \
  --warn-unused-ignores \
  --ignore-missing-imports \
  --disallow-untyped-defs \
  --check-untyped-defs
```

テストコードにも型ヒントとdocstringを付け、製品コードと同じ静的品質基準を適用します。これにより、テストヘルパー自身の座標順序や戻り値の取り違えも早期に検出できます。

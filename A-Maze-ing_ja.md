# A-Maze-ing（日本語版）

*This is the way* — Version 2.2

## 1. 概要

Python で迷路生成器を実装し、設定ファイルを受け取って迷路を生成します。生成結果は壁を16進数で表したファイルに保存し、端末 ASCII または MiniLibX（MLX）で可視化します。迷路生成ロジックは、将来別のプロジェクトから再利用できるパッケージとしても提供します。

迷路生成は、ランダム性・アルゴリズム・グラフ理論を学ぶ題材です。完全迷路（任意の二地点間に経路が一つだけの迷路）は、グラフ理論の全域木に対応します。

## 2. AI 利用に関する指針

- AI は反復的・単調な作業の軽減に使い、生成物は必ず理解して責任を持てるものだけを採用する。
- 問題を整理してから具体的なプロンプトを書き、生成されたコード・説明・文書は検証、レビュー、テストする。
- 仲間によるレビューを必ず行う。AI の出力だけで検証を終えない。
- AI の利用内容は README の Resources 節に、用途と対象箇所を明記する。

## 3. 共通要件

### 3.1 実装規約

- Python **3.10 以上**で実装する。
- `flake8` に準拠する。
- 例外を適切に処理し、設定不正・ファイル不在・不正な構文・不可能なパラメータなどで予期せずクラッシュしないようにする。利用者に明確なエラーメッセージを出す。
- ファイルなどのリソースは `with` 文などのコンテキストマネージャーで確実に管理する。
- 関数の引数・戻り値・必要な変数に型ヒントを付ける。以下のオプションで `mypy` エラーを出さないこと。

```text
mypy . --warn-return-any --warn-unused-ignores --ignore-missing-imports \
  --disallow-untyped-defs --check-untyped-defs
```

- 関数・クラスには PEP 257 準拠（Google / NumPy スタイルなど）の docstring を記載する。
- テスト（`pytest` または `unittest`）を作り、境界値を含めて検証する。テスト自体は提出・採点対象ではない。
- Python の生成物を除外する `.gitignore` を用意する。仮想環境の利用を推奨する。

### 3.2 Makefile

プロジェクト直下に Makefile を置き、少なくとも次を実装します。

| ターゲット | 内容 |
| --- | --- |
| `install` | `pip`、`uv`、`pipx` などで依存関係をインストールする |
| `run` | メインスクリプトを実行する |
| `debug` | Python 組込みデバッガー（例: `pdb`）で実行する |
| `clean` | `__pycache__`、`.mypy_cache` などの一時ファイルを削除する |
| `lint` | `flake8 .` と指定オプション付き `mypy` を実行する |
| `lint-strict`（任意） | `flake8 .` と `mypy . --strict` を実行する |

## 4. 必須機能

### 4.1 実行方法

メインファイル名は必ず `a_maze_ing.py` とし、設定ファイルを唯一の引数として実行します。

```bash
python3 a_maze_ing.py config.txt
```

リポジトリにはデフォルト設定ファイルを必ず含めます。

### 4.2 設定ファイル

設定ファイルは1行につき1組の `KEY=VALUE` です。`#` で始まる行はコメントとして無視します。

| キー | 必須 | 説明 | 例 |
| --- | --- | --- | --- |
| `WIDTH` | 必須 | 迷路の幅（セル数） | `WIDTH=20` |
| `HEIGHT` | 必須 | 迷路の高さ（セル数） | `HEIGHT=15` |
| `ENTRY` | 必須 | 入口座標 `(x,y)` | `ENTRY=0,0` |
| `EXIT` | 必須 | 出口座標 `(x,y)` | `EXIT=19,14` |
| `OUTPUT_FILE` | 必須 | 出力ファイル名 | `OUTPUT_FILE=maze.txt` |
| `PERFECT` | 必須 | 完全迷路にするか | `PERFECT=True` |

必要に応じて `SEED`、`ALGORITHM`、表示モードなどの追加キーを定義して構いません。

### 4.3 迷路の要件

- 迷路はランダム生成するが、**シードによる再現性**を備える。
- 各セルは北・東・南・西に0〜4枚の壁を持つ。
- 有効な迷路であること。
  - 入口と出口が迷路内に存在し、互いに異なる。
  - `42` パターンの閉鎖セルを除き、全セルが連結し孤立セルがない。
  - 入口・出口もセルとして指定するため、外周には壁がある。
  - 隣接する二つのセルで共有壁の状態が一致する（例: あるセルの東壁が閉じていれば、東隣セルの西壁も閉じている）。
- 大きな開放領域を作らない。通路の幅は2セル以下とし、2×3 / 3×2 の開放領域は許可されるが、3×3 は不可。
- 可視化した迷路に、完全に閉じた複数セルで明確な **`42`** を描く。迷路が小さくて配置不能な場合は省略してよいが、コンソールにエラーを表示する。

#### `PERFECT=True` の場合

- 入口から出口までの経路がちょうど一つである。
- すなわちループのない完全迷路とする。

#### `PERFECT=False` の場合（デフォルト）

Pac-Man 風ゲームにそのまま使える盤面にします。

- すべての通路が到達可能で、盤面全体にアイテムを置いてもクリア可能である。
- 四隅と中央は開いた通路にする（四隅はゴースト／パワーアイテム、中央はプレイヤーの開始位置）。
- 少なくとも二つの独立した経路（ループ）を持つ。一つだけ壁を取り除いた完全迷路は不可。
- 行き止まりは少数に抑える。行き止まりゼロはボーナス対象。

### 4.4 出力ファイル形式

迷路を1セルにつき16進数1文字で出力します。ビットが1なら対応する壁は閉鎖、0なら開放です。

| ビット | 方向 |
| --- | --- |
| 0（LSB） | 北 (`N`) |
| 1 | 東 (`E`) |
| 2 | 南 (`S`) |
| 3 | 西 (`W`) |

例: `3`（2進数 `0011`）は北・東が閉鎖、南・西が開放です。`A`（2進数 `1010`）は東・西が閉鎖です。

- セルを行優先で並べ、1行を迷路の1行として出力する。
- 迷路本体の後に空行を1行入れる。
- 続く3行に、それぞれ入口座標、出口座標、入口から出口への最短有効経路を `N` / `E` / `S` / `W` で出力する。
- すべての行を改行文字 `\n` で終える。

配布済みの `maze_analyzer.py` は、共有壁の整合性と、完全迷路またはプレイ可能な盤面の要件を検査できます。

### 4.5 可視化と操作

以下のどちらかで迷路を表示します。

- ターミナルの ASCII 描画
- MiniLibX（MLX）によるグラフィカル表示

表示では壁、入口、出口、解答経路を明確に区別します。最低限、次の操作を提供します。

- 新しい迷路を再生成して表示する。
- 入口から出口までの有効な最短経路を表示／非表示にする。
- 壁の色を変更する。
- （任意）`42` パターンを特定の色で表示する。

## 5. 再利用可能な迷路生成器

- 迷路生成を、単独モジュール内の単一クラス（例: `MazeGenerator`）として実装し、後続プロジェクトから import 可能にする。
- 以下を説明する短いドキュメントを提供する。
  - 生成器の生成と基本利用例
  - サイズ・シードなどカスタム引数の渡し方
  - 生成済み構造と少なくとも一つの解答へのアクセス方法
- 生成器の構造は出力ファイルの形式と同一でなくてもよい。
- リポジトリ直下に、`mazegen-*` という名前の pip インストール可能なパッケージ成果物を置く（`.tar.gz` または `.whl`）。例: `mazegen-1.0.0-py3-none-any.whl`。
- 評価時にソースから再ビルドできるよう、ビルドに必要な全ファイルをリポジトリに含める。
- 再利用・配布を明示的に許可するライセンスを `LICENSE.md` としてリポジトリ直下に置く。
- この短い利用ドキュメントはメインの `README.md` にも含める。

## 6. README.md の必須内容

- 1行目を次のイタリック文にする。

  ```markdown
  *This project has been created as part of the 42 curriculum by <login1>[, <login2>[, <login3>[...]]].*
  ```

- `Description`: 目的と概要。
- `Instructions`: インストール・ビルド・実行方法。
- `Resources`: 参考文献・記事・チュートリアル、および AI を使った作業と対象箇所。
- 設定ファイルの完全な構造と形式。
- 選んだ迷路生成アルゴリズムと、その選定理由。
- 再利用できるコードと再利用方法。
- チーム／プロジェクト管理: 各担当、当初計画と完了時までの変化、良かった点と改善点、利用ツール。
- 複数アルゴリズムや表示オプションなどの発展機能を実装した場合は、その説明。

README は英語推奨ですが、キャンパスの主言語でも構いません。

## 7. ボーナス例

- デフォルト（非完全）モードで行き止まりゼロの完全に編み込まれた盤面を作る（`maze_analyzer.py --max-dead-ends 0` で検証）。
- 複数の迷路生成アルゴリズムに対応する。
- 迷路生成中のアニメーションを追加する。

## 8. 提出・評価

- 通常どおり Git リポジトリで提出する。評価対象はリポジトリ内の成果物のみ。
- 評価中に、理解確認のため短時間でできる小規模な修正を依頼されることがある。関数・表示・データ構造などを数分で説明・変更できるようにしておく。
